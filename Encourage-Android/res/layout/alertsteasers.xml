<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/alertBackgroundImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/page_bg" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/alertBackgroundImageView"
        android:layout_alignLeft="@+id/alertBackgroundImageView"
        android:layout_alignRight="@+id/alertBackgroundImageView"
        android:layout_alignTop="@+id/alertBackgroundImageView" 
        android:layout_above="@+id/buttonView"
        android:padding="15dp">

        <ListView
            android:id="@+id/alertTeaserList"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >
        </ListView>

    </ScrollView>

    <RelativeLayout
        android:id="@+id/buttonView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" >

        <Button
            android:id="@+id/viewAllAlerts"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:text="View all Alerts" 
            android:onClick="viewAllAlertButtonPressed"/>
    </RelativeLayout>

</RelativeLayout>